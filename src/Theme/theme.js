import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#F4005E"
    }
  },
  typography: {
    h3: {
      margin: '30px 0px 0px 0px',
      borderTop: '1px solid',
      paddingTop: '40px',
      width: '400px' 
    }
  },
});
